# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env
.env.*
!.env.example

# Supabase CLI (local state; keys live in .env via FLINT_SUPABASE_* — see .env.example)
supabase/.branches
supabase/.temp

# Local design docs (not published to GitHub)
docs/

# Rust
src-tauri/target/
src-tauri/.fastembed_cache/
evals/target/

# Eval transient outputs — baseline.json is committed, everything else is per-run noise.
evals/results/*.json
evals/results/*.md
!evals/results/baseline.json

# Whisper / other local model weights (downloaded at runtime)
*.ggml
*.bin
models/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
