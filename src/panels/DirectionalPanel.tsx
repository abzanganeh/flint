export interface DirectionalPanelProps {}

const DirectionalPanel = (_props: DirectionalPanelProps) => {
  return <div data-testid="directional-panel">DirectionalPanel placeholder</div>;
};

export default DirectionalPanel;
